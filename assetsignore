# Files in this repo that must NOT be served to the public.
#
# Workers Static Assets reads this file (gitignore format) from the root of
# assets.directory, which for this project is the repo root. This is the
# supported mechanism. The "exclude" key in wrangler.jsonc is NOT a documented
# key for `assets` (it belongs to the deprecated Workers Sites config), so
# treat THIS file as authoritative.
#
# Do NOT add app.js. It is generated by `npm run build` at deploy time and it
# is the bundle the browser loads.

node_modules
src
tests
tools
entry.jsx
worker.js
package.json
package-lock.json
bun.lockb
wrangler.jsonc
.gitignore
.assetsignore
README.md
README_SPLIT.md
